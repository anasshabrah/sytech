/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove or comment out output: 'export'
  // output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
